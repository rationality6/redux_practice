<%= form_for(react) do |f| %>
  <% if react.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(react.errors.count, "error") %> prohibited this react from being saved:</h2>

      <ul>
      <% react.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :phone %>
    <%= f.number_field :phone %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
